using System.IO; 

...

string fileName = "Journal.txt";

using (StreamWriter outputFile = new StreamWriter(filename))
{
    // Add text to the file with the WriteLine method
    outputFile.WriteLine("If I had one thing I could do today.");
    
    //Use the $ and include variables just like with Console.WriteLine
    string sentence = "";
    outputFile.WriteLine($"{sentence}");
}